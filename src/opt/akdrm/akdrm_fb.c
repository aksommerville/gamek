#include "akdrm_internal.h"

/* Init with raw framebuffer, main entry point.
 */
 
int akdrm_init_fb(struct akdrm *akdrm) {
  fprintf(stderr,"TODO %s\n",__func__);
  return -1;
}

/* Swap framebuffers.
 */
 
int akdrm_fb_swap(struct akdrm *akdrm) {
  return -1;//TODO
}
